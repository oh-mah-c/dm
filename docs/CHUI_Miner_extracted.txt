+
Mining Closed High Utility Itemsets without
Candidate Generation
Cheng-Wei Wu1, Philippe Fournier-Viger2, Jia-Yuan Gu3, Vincent S. Tseng1*
1Department of Computer Science, Chiao Tung University, Hsinchu, Taiwan
2Department of Computer Science, University of Moncton, Moncton, Canada
3Department of Computer Science and Information Engineering, Cheng Kung University, Tainan, Taiwan
silvemoonfox@gmail.com, philippe.fournier-viger@umoncton.ca, lester13.gu@gmail.com,
*Correspondence: vtseng@cs.nctu.edu.tw
Abstract—High utility itemsets (HUIs) mining refers to algorithms use the concept of transaction-weighted downward
discovering sets of items that not only co-occur but also carry closure property [7] to find HUIs and consist of two phases
high utilities (e.g., high profits). HUI mining receives extensive named Phase I and Phase II. In Phase I, they generate
attentions in recent years due to the wide applications in various candidates for HUIs by overestimating the utility of itemsets.
domains like commerce and biomedicine. However, huge number In Phase II, HUIs are identified from the candidates by
of HUIs might be produced to users, which degrades the scanning the original database once.
efficiency of the mining process. A promising solution to this Recently, a novel algorithm named HUI-Miner [9] was
problem is to mine closed+ high utility itemset (CHUI), a compact
proposed, which relies on a novel structure named utility-list
and lossless representation of HUIs. Nevertheless, existing
to discover HUIs by using a single phase. The utility-list
algorithms incur the problem of producing a large amount of
structure allows HUI-Miner to directly compute the utility of
candidates, which degrades the mining performance in terms
generated itemsets in main memory without scanning the
of time and space. In this paper, a novel algorithm named CHUI-
original database. Besides, by using this structure, a tighter
Miner (Closed+ High Utility Itemset mining without candidates) for
upper bound on the utility of supersets of an itemset can be
mining CHUIs is proposed, which directly computes the utility of
obtained so that a larger part of the search space can be
itemsets without producing candidates. To our best knowledge,
effectively pruned. To the best of our knowledge, HUI-Miner
this is the first work addressing the issue of mining CHUIs
is the state-of-the-art algorithm for mining HUIs [9].
without candidate generation. Experimental results show
that CHUI-Miner is several orders of magnitude faster than the Although the aforementioned algorithms represent state-
state-of-the-art algorithms. of-the-art techniques for HUI mining, they often present a
large number of HUIs to users. A large number of HUIs and
Keywords—utility mining, closed itemset mining, closed+ high candidates cause the mining tasks to suffer from long
utility itemset, compact and lossless representation execution time and huge memory consumption. The more
itemsets and candidates are generated by the algorithms, the
I. INTRODUCTION more resources they require. When the system resources are
limited (memory, disk space or processing power), it is often
High utility itemset mining has emerged as an important
impractical to generate the entire set of HUIs. Moreover, a
research topic in data mining and has received a lot of
large amount of HUIs is hard to be utilized by users.
attention in recent years. In utility mining, each item in a
To address this problem, Tseng et al. proposed a compact
database has an external utility (e.g., weight / unit profit /
and lossless representation of HUIs [15] called closed+ high
interestingness) and an item in a record has an internal utility
utility itemset (abbreviated as CHUI). An itemset is called
(e.g., purchase quantity / occurrence count / importance). The
closed high utility itemset if its utility is no less than a user-
utility of an itemset is measured by its external utility and
specified minimum utility threshold and it has no proper
internal utility. An itemset is called high utility itemset
supersets having the same support. Although the set of closed
(abbreviated as HUI) if its utility is no less than a user-
HUIs is a compact representation of HUIs, it is not lossless.
specified minimum utility threshold. Mining high utility
To tackle this problem, each closed HUI is annotated with a
itemsets in a database consists of identifying the sets of items
special structure called utility unit array such that the resulting
that co-occur in a database with high utilities (e.g., high
itemset is called a closed+ high utility itemset. The idea of
profits). HUI mining has been considered in a wide range of
utility unit array makes the set of CHUIs lossless because
applications such as cross marketing [2, 5, 14], user behavior
HUIs and their utilities can be derived from this set without
analysis [12] and biomedical applications [3]. Although HUI
accessing the original database. Besides, it was shown that the
mining is essential to many applications, mining HUIs is not
set of CHUIs can be several orders of magnitude smaller than
as easy as mining frequent itemsets due to the absence of
the set of all HUIs, especially for dense databases. Moreover,
downward closure property [1, 4]. The search space for
they proposed the first algorithm named CHUD to discover
mining HUIs cannot be directly reduced as it is done in
CHUIs in databases and an efficient method named DAHU to
frequent itemset mining because a superset of a low utility
recover all the HUIs from the complete set of CHUIs. To our
itemset can be a HUI. Many efficient algorithms were
best knowledge, CHUD is the current best algorithm for
proposed for mining HUIs, such as Two-Phase [7], IHUP [2],
IIDS [18] UP-Growth [13] and UP-Growth+[14]. These mining CHUIs.
Although CHUI mining is desirable and useful in many TABLE 1. AN EXAMPLE DATABASE
applications, it is still a challenging data mining task that has TID Transaction TU
not been deeply explored. To our best knowledge, only the T 1 A(3), B(3), C(2), E(3) 26
CHUD algorithm has been proposed so far for efficiently T 2 B(2), D(7) 20
mining CHUIs. However, the CHUD algorithm has not been T 3 A(2), C(5), D(5), E(2) 25
T A(1), C(6), D(5), E(1), F(2) 29
compared with the most recent algorithms. Therefore the 4
answer to the question “Is it still faster to discover CHUIs TABLE 2. UNIT PROFITS OF ITEMS
than HUIs?” is unknown. Besides, another critical problem Item A B C D E F
Unit Profit 4 3 1 2 1 4
with the CHUD algorithm is that it relies on the two phase
model and overestimates too many low utility itemsets as TABLE 3. TWU VALUES OF ITEMS
candidates, which degrades its overall performance in terms of
Item A C E D B F
execution time and memory usage. In view of these problems, TWU 80 80 80 74 46 29
an interesting question is raised: “Could we design a one-
phase algorithm for efficiently mining CHUIs without TABLE 4. REORGNIZED TRANSACTIONS WHEN MIN_UTIL = 40
producing candidates?”. TID Reorganized Transaction RTU
In fact, there exist several challenges to develop such an T 1 A[12], B[9], C[2], E[3] 26
T B[6], D[14] 20
algorithm. First, designing a CHUI mining algorithm requires 2
T A[8], C[5], D[10], E[2] 25
to correctly integrate techniques from closed itemset mining [6, 3
T A[4], C[6], D[10], E[1] 21
10, 16] with those for HUI mining such that the complete set 4
of CHUIs can be captured with no miss. Second, developing
II. PROBLEM DEFINITION
an algorithm for discovering CHUIs in one phase would
require a special data structure that allows computing the Let be a finite set of distinct items I* = {I 1 , I 2 , …, I N }, such
utility of CHUIs in memory without producing candidates and that each item I i ∈I* is associated with a positive number p(I i ,
performing a costly database scan to calculate their utilities D), called its external utility (e.g. unit profit). A transactional
and utility unit arrays. Lastly, the resulting algorithm is database D = {T , T , …, T } is a set of transactions, where
1 2 M
required to be more efficient in terms of execution time and each transaction T∈D (1 ≤ r ≤ M) is a subset of I* and has an
r
memory than state-of-the-art algorithms for mining CHUI and unique identifier r, called its Tid. In a transaction T, each item
r
HUIs. Lastly, the resulting algorithm may be slower or less I∈I* is associated with a positive number q(I, T), called its
i i r
memory efficient than current state-of-the-art algorithms for internal utility in T (e.g. purchase quantity). An itemset X =
r
mining CHUI and HUIs. {I , I , …, I} is a set of k distinct items, where I∈I* (1 ≤ i ≤
1 2 k i
In this paper, we address all the above challenges by k) and k is the length of X. A k-itemset is an itemset of length
proposing a new framework for mining CHUIs without k. An itemset X is said to be contained in a transaction T if
r
candidate generation, a topic that has not been explored so far. X⊆T.
r
The contributions of this work are summarized as follows. For example, TABLE 1 shows an example database
First, we propose a novel structure named EU-List containing five transactions. Each row in TABLE 1 represents a
(Extended Utility-List) to maintain the utility information of transaction, in which each letter represents an item and has a
itemsets in transactions, which allows to efficiently calculate purchase quantity (internal utility). The unit profit (external
the utility and utility unit arrays of itemsets in memory utility) of each item is shown in TABLE 2.
without scanning the original database.
Second, a novel algorithm named CHUI-Miner (Closed+ Definition 1 (Superset and subset). Let be non-empty
itemsets X and Y. X is a subset of Y, or equivalently Y is a
High Utility Itemset mining without candidates) incorporating
the EU-List structure is proposed, which adopts the divide- superset of X if X⊆Y. If X⊂Y, X is a proper subset of Y and Y
and-conquer methodology to mine the complete set of CHUIs is a proper superset of X.
in databases without producing candidates.
Definition 2 (Support count and Tidset). The support count
Third, we perform an extensive experimental study on both
of an itemset X is the number of transactions containing X in D
real and synthetic datasets to evaluate the performance of the
and is denoted as SC(X). The Tidset of an itemset X is denoted
proposed algorithm. Experimental results show that CHUI-
as TidSet(X) and defined as the set of Tids of transactions
Miner outperforms the current best algorithm for mining
containing X. The support count of an itemset X is expressed
CHUIs substantially, especially for dense datasets. Moreover,
in terms of TidSet(X) as SC(X) = |TidSet(X)|.
the combination of CHUI-Miner and DAHU is also faster than
state-of-the-art algorithms for mining HUIs even on sparse For example, TidSet({ACE}) = {1, 3, 4} and SC({ACE})
datasets. More importantly, CHUI-Miner is more than two = |TidSet({ACE})| = 3.
orders of magnitude faster than the compared algorithms on
Property 1. For a k-itemset X = {I , I ,…, I}, SC(X) =
dense datasets. 1 2 k
|TidSet(I ) ∩ TidSet(I ) ∩ …∩ TidSet(I)|.
The rest of this paper is organized as follows. Section 2 1 2 k
presents preliminaries and defines the problem of closed+ high Property 2. Let itemset Y be a proper superset of X. Then,
utility itemset mining. Section 3 describes the CHUI-Miner TidSet(Y) ⊆ TidSet(X).
algorithm in detail. An extensive performance evaluation is
presented in Section 4. Section 5 draws the conclusion.
Definition 3 (Utility of an item in a transaction). The utility Property 3. For any itemset X, the support count of X is equal
of an item I∈I* in a transaction T is denoted as u(I, T) and to that of its closure. Equivalently, SC(X) = max{SC(Y) | Y is
i r i r
defined as p(I, D) × q(I, T). closed and X ⊆ Y}.
i i r
Definition 4 (Utility of an itemset in a transaction). The For example, the support count of {AC} is equal to that of
utility of a k-itemset X= {I , I ,…, I} in a transaction T is its closure SC(closure({AC})) = 3. Besides, SC({AC}) =
1 2 k r
denoted u(X, T) and defined∑ u(I ,T ). max{SC({ACE}), SC({ABCE}), SC({ACED}),
r Ii ∈ X i r SC({ACDEF})} = 3.
For example, the utility of {A} in T 1 is u({A}, T 1 ) = p({A}, Definition 11 (Closed high utility itemset). An itemset X is
D) × q({A}, T 1 ) = 4 × 3= 12. The utility of {ACE} in T 1 is called closed high utility itemset iff X is closed and u(X) ≥
u({ACE}, T ) = u({A}, T ) + u({C}, T ) + u({E}, T ) = 12 + 2
1 1 1 1 min_util. The complete set of closed HUIs is defined as CH =
+ 3 = 17.
H ∩ C.
Definition 5 (Transaction utility). The transaction utility
For example, if min_util = 30, the complete set of closed
(abbreviated as TU) of a transaction T is denoted as TU(T)
r r HUIs in TABLE 1 is CH = H ∩ C = {{D}, {ACE}, {ACDE}}.
and defined as u(T, T).
r r
Definition 12 (Utility unit array). For a k-itemset X = {I ,
1
Definition 6 (Total utility). The total utility of a database D
I , …, I}, its utility unit array is denoted as V(X) = [v , v ,…,
∑ 2 k 1 2
is denoted as TotalU and defined as TU(T ,T ). v], where the k-th utility value in V(X) is defined as V(X, I)
D Tr∈ D r r k i
∑
= u(I ,T ). Besides, the utility value of X
For example, the transaction utility of T is TU(T ) = r ∈ TidSet(X)∧ Ii∈ Tr i r
1 1
u({ABCE}, T
1
) = 26. The total utility of the database in TABLE can be expressed as u(X) = ∑
i
k
=1
V(X,I
i
).
1 is TotalU = TU(T ) + TU(T ) + TU(T ) + TU(T ) = 26 + 20
D 1 2 3 4
+ 25 + 29 = 100. For example, the second utility value of the utility unit
array of {ACE} is V({ACE},{C}) = u({C}, T ) + u({C}, T ) +
Definition 7 (Utility and relative utility of an itemset). The 1 3
v({C}, T ) = 2 + 5 + 6 = 13. The utility unit array of {ACE} is
utility of an itemset X in the database D is defined as u(X) = 4
V({ACE}) = [24, 13, 6] and u({ACE}) = V({ACE}, {A}) +
∑
r ∈ TidSet(X) u(X,T r ). The relative utility of X is defined as V({ACE}, {C}) + V({ACE}, {E}) = 43.
ru(X) = u(X)/TotalU D . Definition 13 (Closed+ high utility itemset). A closed high
For example, the utility of {AC} is u({AC}) = u({AC}, T ) utility itemset X is called closed+ high utility itemset
1
+ u({AC}, T ) + u({AC}, T ) = 17 + 15 + 11 = 43. The (abbreviated as CHUI) iff X is attached with its utility unit
relative utility 3 of {AC} is ru({ 4 AC}) = 43/100 = 43%. array. The complete set of CHUIs is denoted as CH+.
Definition 8 (High utility itemset). An itemset X is called For example, CH+ = {({D}, [34]), ({ACE}, [24, 13, 6]),
high utility itemset (abbreviated as HUI) if u(X) is no less than {ACDE}, [12, 11, 20, 3]}.
a user-specified minimum utility threshold min_util (0 <
Property 4. For any high utility itemset X, there exists a
min_util ≤ TotalU D ). Otherwise, the itemset is low utility. Let CHUI itemset Y such that Y = closure(X) and u(Y) ≥ u(X).
min_util/TotalU be the relative minimum utility threshold
D
rmin_util. An equivalent definition is that X is a HUI iff ru(X) Property 5. For any itemset X that is not a CHUI, all its
≥ rmin_util. The complete set of HUIs in D is denoted as H. subsets are low utility.
For example, if min_util = 30, the set of HUIs in TABLE 1 Property 6. The complete set of CHUIs is a lossless
is H = {{D}:34, {AC}:37, {AD}:32, {AE}:30, {CD}:31, representation of all HUIs. For any k-itemset X = {I 1 , I 2 ,…, I k },
{ACD}:43, {ACE}:43, {CDE}:34, {ADE}: 35, {ACDE}:46}, if closure(X)∈CH+, u(X) can be calculated as
where the number beside each itemset is its utility. ∑ V(closure(X),I ) by using the utility unit array of its
Ii∈ X i
Definition 9 (Closed itemset). An itemset X is called closed closure without accessing the original database.
itemset if there exists no proper superset Y ⊃ X in D such that
Problem Statement. Given a database D with internal and
SC(X) = SC(Y). The complete set of closed itemset is denoted
external utility of items, and a user-specified minimum utility
as C.
threshold min_util, the problem statement is to discover from
For example, the complete set of closed itemset in TABLE 1 D all closed itemsets whose utilities are no less than min_util
is C = {{B}:2, {D}:3, {BD}:1, {ACE}:3, {ABCE}:1, and their utility unit arrays.
{ACED}:2, {ACDEF}:1}, in which the number beside each
itemset is its support count.
Definition 10 (Closure of an itemset). Let Y be a superset of
an itemset X. Y is called the closure of X if Y is closed and
SC(Y) = SC(X). The closure of X is defined as closure(X) =
∩ r∈TidSet(X) T r .
For example, the closure of {AC} is closure({AC}) = T ∩ Fig. 1. The pseudo code of the CHUI-Miner algorithm
1
T ∩ T = {ABCE} ∩ {ACDE} ∩ {ACDEF} = {ACE}.
3 4
III. THE PROPOSED METHOD: CHUI-MINER multiplication of its internal and external utilities.
In this section, we propose an efficient algorithm for {{AA}} {{CC}} {{EE}} {{DD}} {{BB}}
discovering CHUIs without producing candidates, namely TTiidd EEUU RRUU TTiidd EEUU RRUU TTiidd EEUU RRUU TTiidd EEUU RRUU TTiidd EEUU RRUU
CHUI-Miner (Closed+ High Utility Itemset mining without 11 1122 1144 11 22 1122 11 33 99 22 1144 66 11 99 00
candidates). The CHUI-Miner algorithm adopts the proposed 33 88 1177 33 55 1122 33 22 1100 33 1100 00 22 66 00
structure EU-List (Extended Utility-List) to maintain the utility 44 44 1177 44 66 1111 44 11 1100 44 1100 00
information of itemsets in transactions. The pseudo code of Fig. 2. Utility-lists of promising items
CHUI-Miner is shown in Fig. 1. The algorithm has two input
parameters: (1) a database D and (2) a user specified minimum 3.2 Generation of CHUIs by using EU-Lists
utility threshold min_util. It outputs the complete set of CHUIs After scanning the database twice, the EU-List of each
in D. The proposed algorithm consists of two parts: (1) promising item is constructed. In the proposed algorithm, each
construction of EU-Lists of promising items and (2) item(set) is associated with an EU-List. The EU-List of an
generation of CHUIs by using EU-Lists. item(set) X includes a utility-list [9], support count of X, utility
unit array of X, and two ordered sets named PrevSet(X) and
3.1 Construction of EU-Lists of promising items
PostSet(X). The utility-list of X consists of several tuples. Each
To efficiently mine CHUIs in a database without
tuple in the utility-list of X represents the utility information of
producing candidates and avoid repeatedly scanning the
X in the reorganized transaction T and has three fields: Tid,
original database, the information of itemsets in transactions r
EU and RU. Fields Tid and EU respectively indicate the
are maintained in EU-Lists. The construction of the initial EU-
identifier of T and the exact utility of X in T. Field RU
r r
Lists can be performed with two database scans(line 1 of Fig.
indicates the remaining utility of X in T. The concept of
r
1). In the first database scan, the transaction utility of each
remaining utility is based on the following definitions.
transaction and TWU of items are calculated.
Definition 17 (Precede and succeed). Given a set of items {I ,
1
Definition 14 (TWU of an itemset). The TWU (Transaction- I ,…, I } and a total order relation R: I ≺ I ≺…≺ I among
2 N 1 2 N
Weighted Utilization) of an itemset X is the sum of the items. An item I precedes an item I if I ≺ I (1≤ i < j ≤ N).
i j i j
transaction utilities of all the transactions containing X, which Otherwise, I succeeds I. In the rest of the paper, items in
i j
is defined as TWU(X) = ∑ TU(T ) . For example, itemsets are sorted according to R.
r∈ TidSet(X) r
TABLE 3 shows the TWU of items. Definition 18 (Remaining utility of an itemset in a
transaction). The remaining utility of a k-itemset X = {I ,
Definition 15 (TWDC property). The TWDC (Transaction- 1
I , …, I}, in a reorganized transaction T is denoted as RU(X,
Weighted Utilization Downward Closure) property [2, 7] 2 k r
T) and defined as ∑ u(I ,T ).
states that for any itemset X if its TWU(X) is less than min_util, r Ik∈ Tr ∧ Ik≺Ii i r
all supersets of X are low utility. Definition 19 (Remaining utility of an itemset in a
database). The remaining utility of a k-itemset X = {I , I , …,
After scanning the database once, items and their TWU 1 2
I} in a database D is denoted as RU(X) and defined as
values are obtained. By the TWDC property, if the TWU of an k
∑ RU(I ,T ).
item is less than min_util, all its supersets are not CHUIs. r∈ TidSet(Ii) i r
These items are called unpromising items. Definition 16 gives
Definition 20 (Utility-list structure). The utility-list of an
a formal definition for unpromising items and promising items
itemset X is denoted as UL(X), which is a list containing
[13, 14, 15]. |TidSet(X)| tuples of the form <Tid, EU, RU>. Each tuple is
called an element and maintains the utility information of X in
Definition 16 (Promising and unpromising items). An item
∈
I∈I* (1 ≤ i ≤ N) is a promising item if TWU(I) ≥ min_util. the transaction T r , where r TidSet(X). An element associated
i i
with T in utility-list of X is denoted as UL(X, T), which
Otherwise, the item is called an unpromising item. r r
contains the information <r, EU(X, T), RU(X, T)>. The EU
r r
During the second database scan, when a transaction T j (1 and RU of the element are respectively denoted as UL(Y,
≤ j ≤ |D|) is retrieved, unpromising items are removed from T j Tr).EU and UL(Y, Tr).RU. For example, Fig. 2 shows the
and their utilities are eliminated from the transaction utility of utility-lists of promising items for the database of Table 1.
T since only supersets of promising items can be CHUIs. The
j
Definition 21 (Extension of an itemset). Let X = {x , x , … ,
promising items in T j are sorted in TWU descending order. ∈ ∈ 1 2
x } (x I*, 1 ≤ i ≤ u) and Y = {y , y , … ,y } (y I*, 1 ≤ j ≤ v)
Note that other orders can be used. In this paper, we use the u i 1 2 v j
be two itemsets. Y is an extension of X if X ⊂ Y and each item
TWU descending order for the running example. A transaction
y ∉ X succeeds all items in X according to the total order R.
resulting from the above process is called a reorganized j
transaction and its transaction utility is called RTU For example, if R is A≺C≺E≺D≺B≺F, {ACE} is not an
(Reorganized Transaction Utility). The RTU of a reorganized extension of {AE} but it is an extension of {AC}.
transaction T is denoted as RTU(T), where 1 ≤ r ≤ |D|.For
r r Definition 22. In the utility-list of an itemset X, the sums of all
example, TABLE 4 shows reorganized transactions for the
its EU and RU values are denoted as SumEU(X) and
database in TABLE 1, where the number beside each item is the
SumRU(X), respectively.
Procedure: ClosureComputation
Input: An itemset Y, PostSet(YC), PostSet(X);
Result: Return Y’s closure YC. Update PostSet(YC) and EU-List of YC;
01. YC ← Y;
02. PostSet(YC) ← Ø;
03. For each item Z∈PostSet(X) do
04. If(TidSet(YC) ⊆ TidSet(X))
05. YC ←YC ∪ Z;
06. For each Tid r∈TidSet(YC) do
07. EL(YC, Tr).EU ← EL(YC, Tr).EU + EL(Z, Tr).EU;
08. EL(YC, Tr).PU ← EL(YC, Tr).PU – EL(Z, Tr).PU;
09. else PostSet(YC) ←PostSet(YC) ∪ Z;
10. Return YC;
Fig. 5. The pseudo code of the ClosureComputation procedure
If the sum of sumEU(Y) and sumRU(Y) is less than
min_util, the combination of Y with any item I∈PostSet(Y) is
low utility. Otherwise, the algorithm calls procedure
IsSubsumedCheck(Y, PreSet(Y)) to check whether Y is
subsumed by an already mined CHUI.
Fig. 3. The pseudo code of the GEN-CHUI procedure Definition 15 (Subsume). The itemset Y is subsumed by the
itemset S if Y ⊂ S and SC(Y) = SC(S). For example, {C} is
subsumed by {ACE} because {C} ⊂ {ACE} and SC({C}) =
SC({ACE}).
Property 8. Given two itemsets Y and S, if Y ⊂ S and SC(Y) =
SC(S) then closure(X) = closure(Y).
Property 9. Given an itemset X and an item I ∈ I* (1 ≤ i ≤ N),
i
Fig. 4. The pseudo code of the IsSubsumedCheck procedure TidSet(X) ⊆ TidSet (I) ⇔ I ∈ closure(X).
i i
Property 7. If SumEU(X) + SumRU(X) is less than min_util,
By Definition 15, if we can find an already mined CHUI S
all extensions of X are low utility.
that subsumes Y, we can conclude that Y is not closed and
The procedure GEN-CHUI(Ø, Ø, P, min_util) is then closure(S) = closure(Y). Hence, we can safely prune the
called to generate CHUIs by using EU-Lists of promising itemset Y and stop exploring the search space of succeeding
items. The procedure is an extension of an efficient algorithm supersets of Y. Otherwise, the procedure returns true.
named DCI-Closed [6] for mining closed itemsets. For each The pseudo code of the IsSubsumedCheck procedure is
closed itemset X discovered by the algorithm, the algorithm shown in Fig. 4. The procedure has two input parameters: (1)
constructs its EU-List to calculate its utility to determine an itemset Y and (2) PrevSet(Y) and it is performed as follows.
whether it is a CHUI and uses property of remaining utility to For each item J in PrevSet(Y), if TidSet(Y) is contained in
prune the search space. The procedure has four input TidSet(J), the procedure returns true to indicate that Y is
parameters: (1) an itemset X, (2) PrevSet(X), (3) PostSet(X) subsumed by an already mined CHUI and thus that Y is not a
and (4) the minimum utility threshold min_util. It explores the CHUI (Property 8 and 9). If TidSet(Y) is not contained in the
search space of CHUIs that are supersets of X by appending TidSet of any item in PostSet(Y), the procedure returns false to
items from PostSet(X) to X. indicate that the closure of Y is closed.
The pseudo code of GEN-CHUI is shown in Fig. 3, which If Y passes the subsumption check, the procedure
is performed as follows. While PostSet(X) is not null, the ClosureComputation(Y, PostSet(Y ), PostSet(X)) is called to
C
procedure selects an item I having the smallest order in compute the closure of Y and construct its EU-List by updating
PostSet(X) to create an itemset Y = X ∪ I and remove I from the EU-List of Y. The pseudo code of the ClosureComputation
PostSet(X) (line 3 to 5 of Fig. 3). Then, the EU-List of Y is procedure is shown in Fig. 5, and it is performed as follows.
constructed as follows. First, TidSet(Y) is obtained by Initially, a variable Y for storing the closure of Y is set to Ø
C
intersecting TidSet(X) and TidSet(I) and the support count of Y and EL(Y ) is set to EL(Y). Then, for each item Z in
C
is set to |TidSet(Y)|. Then, the utility-list of Y is obtained by the PostSet(X), we check if TidSet(Y) is contained in Tidset(Z). If
following process. For each transaction it is not contained in Tidset(Z), Z is added to PostSet(Y).
∈
T TidSet(X)∩TidSet(I), the element UL(Y, T) is created in Otherwise, Z is added to Y because Z is contained in the
r r C
UL(Y), where UL(Y, T).EU is set to the sum of UL(X, T).EU closure of Y by Property 9.
r r
and UL(I, T).EU, and where UL(Y, T).RU is set to UL(I, The EU-List of Y is updated by the following process. For
r r C
T).RU. Finally, PrevSet(Y) is initialized to PrevSet(X). In each Tid r∈ TidSet(Y ) ∩ TidSet(Z), the EL(Y , T) is updated
r C C r
other word, UL(Y, T).EU = UL(X, T).EU + UL(I, T).EU and to <r, v , v >, where v is EL(Y , T).EU + EL(Z, T).EU and v
r r r 1 2 1 C r r 2
UL(Y, T).RU = UL(X, T).RU – UL(I, T).EU. Finally, is EL(Y , T).PU – EL(Z, T).PU. After processing all the items
r r r C r r
PrevSet(Y) is initialized to PrevSet(X). in PostSet(X), the EU-List of Y is updated and Y captures the
C C
closure of Y. Then, the procedure returns Y’s closure Y .
C
After calling the ClosureComputation procedure, the TABLE 5. STATISTICAL INFORMATION ABOUT DATASETS
UpdateUtilityArray(Y ) procedure is then called to calculate
C
the utility unit array of Y . If Y = {I , I ,..., I}, the utility unit
C C 1 2 k
array V(Y ) of Y is calculated as follows. For each item I
C C i
∈Y , we calculate V(Y , I) (Definition 12) by scanning UL(I)
C C i i
once to sum up the utilities of I in all elements whose Tids are
i
in Tidset(Y ). Note that in our implementation for any item
C
I∈Y such that I ∉Y, the utility value V(I, Y ) is 4.1 Experiments on dense datasets
C C
calculated during the closure computation by the
The first experiment consists of running the algorithms on
ClosureComputation procedure. For the sake of simplicity,
dense datasets Mushroom, Connect and Chess while varying
this optimization is not shown in the pseudo code of the
the min_util parameter. Execution times of the compared
ClosureComputation procedure due to the page limitation.
algorithms are shown in Fig 6. (a), (b), (c). The results show
Then the algorithm outputs Y if SumEU(Y ) is no less
C C that CHUI-Miner and CHUI-Miner/DAHU outperform both
than min_util because Y meets the criteria of being a closed
C CHUD and HUI-Miner for their respective tasks. For example,
itemset, a high utility itemset, and is associated with its utility
when min_util = 1% for the Mushroom dataset, CHUI-Miner
unit array. In other word, Y is a CHUI. The algorithm then
C and CHUI-Miner/DAHU are more than 100 times faster than
calls the GEN-CHUI procedure to further explore the search
CHUD and HUI-Miner. The reason why the former algorithms
space and find the CHUIs that are succeeding supersets of Y .
C performs so well is that CHUD and HUI-Miner respectively
Then, the item I is added into PrevSet(X). When the CHUI-
produce a large number of candidates and HUIs, as shown in
Miner algorithm terminates, all the CHUIs in the database are
TABLE 6 and Fig 7. (a). Another insightful observation is that
obtained.
mining CHUIs is not always faster than mining HUIs (it
depends on the algorithm design). For example, CHUD is
slower than HUI-Miner on Connect for low min_util values.
IV. EXPERIMENTAL EVALUATIONS
The reason is that CHUD produces too many candidates. For
In this section, we compare the performance of CHUI- example, for Chess and min_util = 10%, CHUD produces
Miner with two algorithms, CHUD [15] and HUI-Miner [9]. more than 50 GB of candidates and cause our computer to run
CHUD mines closed+ high utility itemsets and HUI-Miner out of disk space.
mines high utility itemsets. These algorithms are to our
4.2 Experiments on sparse datasets
knowledge the best algorithms for their respective mining
The second experiment consists of running the algorithms
tasks. Moreover, to compare CHUI-Miner with HUI-Miner
on sparse datasets. Execution times of the algorithms are
which produces different results, we have added a version of
shown in Fig 6. (d), (e), (f) and the number of HUIs,
CHUI-Miner combining CHUI-Miner and DAHU, called
CHUI-Miner+DAHU, for mining high utility itemsets.
candidates and CHUIs are given in TABLE 6 and Fig 7. (b), (c).
Results show that CHUI-Miner and CHUI-Miner/DAHU
Experiments are performed on a computer with an Intel Core
outperform both CHUD and HUI-Miner again, despite that the
i7-2600 CPU @ 3.40 GHz, 8 GB of RAM, and Windows 7
performance gap is smaller than for dense datasets. For
SP1, 64-bit version. All algorithms are written in Java, and run
example, when min_util = 0.005% for the Foodmart dataset,
on JVM 1.7.0_45-b18. Both the initial and maximum heap
CHUI-Miner and CHUI-Miner/DAHU are about 6 times faster
memory sizes of the JVM are set to 2,048 MB.
than CHUD and 21 times faster than HUI-Miner. Because the
To analyze the performance of the algorithms in different
number of closed+ high utility itemset becomes very small
situations, we test the algorithms with six different datasets.
compared to the number of high utility itemsets for the
Their characteristics are shown in TABLE 5. The datasets
Mushroom, Connect, Chess and Retail are obtained from the Foodmart dataset when min_util is lower than 0.06%, CHUD
FIMI Repository [20], Foodmart is obtained from the becomes faster than HUI-Miner. But still, it can be observed
Microsoft foodmart 2000 database [21] and Chain Store is that CHUD is slower than HUI-Miner on the Retail and
obtained from NUMineBench 2.0 [11]. Except for Foodmart ChainStore datasets for low min_util values, and CHUI-Miner
and Chain Store, these datasets do not provide the external and and CHUI-Miner/DAHU remain faster than both of them. The
reasons why these latter are still faster than HUI-Miner is that
internal utilities of items. As in the performance evaluation of
previous studies [9, 13, 14], the external utilities of items are (1) CHUI-Miner performs a two way search to build utility
generated between 0.01 and 10 using a log-normal distribution lists unlike HUI-Miner which performs a three-way search and
and the internal utilities of items are generated randomly (2) the closure of itemsets are constructed directly by CHUI-
ranging from 1 to 5. Miner, thus avoiding calculating utility lists of several low
utility or non-closed itemsets. This result is interesting since it
is contrary to the observation in the field of frequent pattern
mining that mining closed itemsets is slower than mining all
frequent itemsets for sparse datasets. Moreover, as show in Fig
6, CHUI-Miner has good scalability even on large datasets.
(a) Mushroom (b) Connect (c) Chess
(d) Foodmart (e) Retail (f) ChainStore
Fig. 6. Execution time on different datasets
(a) Mushroom (b) Foodmart (c) Retail
Fig. 7. Number of itemsets on different datasets
(a) Connect (b) Retail (c) ChainStore
Fig. 8. Memory consumption on different datasets
TABLE 6. NUMBERS OF HIGH UTILITY ITEMSETS, CANDIDATES PRODUCED BY References
CHUD AND CLOSED HIGH UTILITY ITEMSETS ON DIFFERENT DATASETS
[1] R. Agrawal and R. Srikant, “Fast algorithms for mining association
rules,” Proc. of the 20th Int'l Conf. on Very Large Data Bases, pp. 487-
499, 1994.
[2] C. F. Ahmed, S. K. Tanbeer, B.-S. Jeong, and Y.-K. Lee, “Efficient tree
structures for high utility pattern mining in incremental databases,”
IEEE Transactions on Knowledge and Data Engineering, vol. 21, issue
12, pp. 1708-1721, 2009.
[3] R. Chan, Q. Yang, and Y. Shen, “Mining high utility itemsets,” Proc. of
IEEE Int'l Conf. on Data Mining (ICDM), pp. 19-26, Nov., 2003.
[4] J. Han, J. Pei, and Y. Yin, “Mining frequent patterns without candidate
generation,” Proc. of the ACM SIGMOD Int'l Conf. on Management of
Data, pp. 1-12, 2000.
[5] H.-F. Li, H.-Y. Huang, Y.-C. Chen , Y.-J. Liu, and S.-Y. Lee, “Fast and
memory efficient mining of high utility itemsets in data streams,” Proc.
of the IEEE Int'l Conf. on Data Mining (ICDM), pp. 881-886, 2008.
[6] C. Lucchese, S. Orlando, and R. Perego, “Fast and memory efficient
4.3 Memory usage comparisons mining of frequent closed itemsets,” IEEE Transactions on Knowledge
and Data Engineering, vol. 18, issue 1, pp. 21-36, 2006.
Fig. 8 shows the memory consumption of the algorithms
[7] Y. Liu, W. Liao, and A. Choudhary, “A fast high utility itemsets mining
on three datasets carrying different characteristics, including a
algorithm,” Proc. of the Utility-Based Data Mining Workshop, pp. 90-
dense dataset (i.e., Connect), a sparse dataset (i.e., Retail) and
99, 2005.
a large scale dataset (i.e., ChainStore). In Fig. 8, we can see
[8] Y.-C. Li, J.-S. Yeh, and C.-C. Chang, “Isolated items discarding strategy
that CHUI-Miner uses less memory than CHUD to find
for discovering high utility itemsets,” Data & Knowledge Engineering,
CHUIs, especially for dense dataset. The reason is that the vol. 64, issue 1, pp. 198-217, Jan., 2008.
number of CHUIs is considerably smaller than the total [9] M. Liu and J. Qu, “Mining High Utility Itemsets without Candidate
number of HUIs for low min_util values. Besides, we have Generation,” Proc. of ACM Int'l Conf. on Information and Knowledge
found that CHUD consumes less memory than CHUI-Miner in Management (CIKM), pp. 55-64, 2012.
several cases because the local TU table, the main data [10] N. Pasquier, T. Bastide, R. Taouil, and L. Lakhal, “Discovering frequent
structure of CHUD, stores less information than utility closed itemsets for association rules,” Proc. of the Int'l Conf. on
Database Theory (ICDT), pp. 398–416, 1999.
lists/EU-lists, the main structures used by HUI-Miner and
[11] J. Pisharath, Y. Liu, B. Ozisikyilmaz, R. Narayanan, W. K. Liao, A.
CHUI-Miner. But for the ChainStore dataset, the advantage
Choudhary, and G. Memik, “NU-MineBench version 2.0 dataset and
provided by mining CHUIs is small, so the memory usage of
technical report,”
CHUI-Miner and CHUD is similar to HUI-Miner. We can also
http://cucis.ece.northwestern.edu/projects/DMS/MineBench.html
observe that the memory usage of CHUI-Miner is more stable
[12] B.-E. Shie, P. S. Yu, and V. S. Tseng, “Mining interesting user behavior
than the memory usage of HUI-Miner and CHUD.
patterns in mobile commerce environments,” Applied Intelligence, vol.
38, issue 3, pp. 418-435, 2013.
V. CONCLUSION [13] V. S. Tseng, C.-W. Wu, B.-E. Shie, and P. S. Yu, “UP-Growth: an
efficient algorithm for high utility itemset mining,” Proc. of ACM
In this paper, we have proposed a new framework for mining SIGKDD, pp. 253–262, 2010.
CHUIs without candidate generation. A novel structure named [14] V. S. Tseng, B.-E. Shie, C.-W. Wu, and P. S. Yu, “Efficient algorithms
EU-List (Extended Utility-List) is proposed to maintain the for mining high utility itemsets from transactional databases,” IEEE
utility information of itemsets in transactions, which allows to Transactions on Knowledge and Data Engineering, vol. 25, issue 8, pp.
1772-1786, 2013.
efficiently calculate the utility and utility unit arrays of
[15] V. S. Tseng, C.-W. Wu, P. Fournier-Viger and P. S. Yu, “Efficient
itemsets in one phase. Moreover, a novel algorithm named
Algorithms for Mining the Concise and Lossless Representation of High
CHUI-Miner (Closed+ High Utility Itemset mining without
Utility Itemsets,” IEEE Transactions on Knowledge and Data
candidates) incorporating the EU-List structure is proposed, Engineering, vol. 27, issue 3, pp. 726-739, 2015.
which discovers the complete set of CHUIs in databases [16] V. S. Tseng, C.-W. Wu, J.-H. Lin and P. Fournier-Viger, , “UP-Miner: A
without producing candidates. Experimental results on real- Utility Pattern Mining Toolbox, accepted and to appear in Proc. of IEEE
Int'l Conf. on Data Mining (ICDM), 2015.
life datasets show that CHUI-Miner can be more than two
http://idb.csie.ncku.edu.tw/tsengsm/software.htm
orders of magnitude faster than the state-of-the-art CHUI
[17] C.-W. Wu, B.-E. Shie, V. S. Tseng, and P. S. Yu, “Mining top-k high
mining algorithm [15]. The implementation of CHUI-Miner
utility itemsets,” Proc. of ACM SIGKDD, pp. 78-86, 2012.
has been incorporated into the open-source high utility pattern
[18] H. Yao, H. J. Hamilton, and L. Geng, “A unified framework for utility-
mining toolbox UP-Miner [16]. The source codes of CHUI- based measures for mining itemsets,” Proc. of ACM SIGKDD 2nd
Miner and the compared algorithms [9, 15] are public Workshop on Utility-Based Data Mining, pp. 28-37, 2006.
available at: http://bigdatalab.cs.nctu.edu.tw/software.htm. [19] M. J. Zaki and C. J. Hsiao, “Efficient algorithms for mining closed
itemsets and their lattice structure,” IEEE Transactions on Knowledge
and Data Engineering, vol. 17, issue 4, pp. 462–478, 2005.
[20] Frequent Itemset Mining Implementations Repository,
http://fimi.cs.helsinki.fi/
[21] FoodMart2000, Microsoft Developer Network (MSDN)
http://msdn.microsoft.com/enus/library/aa217032(v=sql.80).asp
